import Layout from "@/Layouts/Layout";
import React from "react";

const Profil = ({ title }) => {
    return (
        <Layout title={title}>
            <h2>Profil</h2>
        </Layout>
    );
};

export default Profil;
